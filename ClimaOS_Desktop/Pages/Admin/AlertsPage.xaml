<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:m="clr-namespace:ClimaOS_Desktop.Models"
             x:Class="ClimaOS_Desktop.Pages.Admin.AlertsPage"
             Style="{StaticResource AdminPage}"
             Title="Alerte meteo">

    <Grid Padding="20" RowDefinitions="Auto, Auto, Auto, *" RowSpacing="14">

        <Grid Grid.Row="0" ColumnDefinitions="Auto, *, Auto" ColumnSpacing="12">
            <Button Grid.Column="0" Text="← Înapoi" Style="{StaticResource AdminGhostButton}" Clicked="OnBackClicked" />
            <Label Grid.Column="1" Text="Alerte meteo" Style="{StaticResource AdminTitle}" VerticalOptions="Center" />
            <Button Grid.Column="2" Text="+ Adaugă" Style="{StaticResource AdminPrimaryButton}" Clicked="OnAddClicked" />
        </Grid>

        <Grid Grid.Row="1" ColumnDefinitions="*, 200, Auto, Auto" ColumnSpacing="10">
            <Border Grid.Column="0" Style="{StaticResource AdminFieldBorder}">
                <Entry x:Name="SearchEntry" Style="{StaticResource AdminEntry}"
                       Placeholder="Caută în titlu / mesaj / locație..." />
            </Border>
            <Border Grid.Column="1" Style="{StaticResource AdminFieldBorder}">
                <Picker x:Name="SeverityPicker" Style="{StaticResource AdminPicker}" Title="Severitate min.">
                    <Picker.Items>
                        <x:String>Toate severitățile</x:String>
                        <x:String>Informare+</x:String>
                        <x:String>Avertisment+</x:String>
                        <x:String>Sever+</x:String>
                        <x:String>Extrem</x:String>
                    </Picker.Items>
                </Picker>
            </Border>
            <Button Grid.Column="2" Text="Caută" Style="{StaticResource AdminPrimaryButton}" Clicked="OnSearchClicked" />
            <Button Grid.Column="3" Text="Reset" Style="{StaticResource AdminGhostButton}" Clicked="OnResetClicked" />
        </Grid>

        <Grid Grid.Row="2" ColumnDefinitions="Auto, *, Auto, *" ColumnSpacing="10">
            <Label Grid.Column="0" Text="De la:" Style="{StaticResource AdminMuted}" VerticalOptions="Center" />
            <Border Grid.Column="1" Style="{StaticResource AdminFieldBorder}">
                <DatePicker x:Name="FromPicker" />
            </Border>
            <Label Grid.Column="2" Text="Până la:" Style="{StaticResource AdminMuted}" VerticalOptions="Center" />
            <Border Grid.Column="3" Style="{StaticResource AdminFieldBorder}">
                <DatePicker x:Name="ToPicker" />
            </Border>
        </Grid>

        <Border Grid.Row="3" Style="{StaticResource AdminCard}" Padding="0">
            <Grid RowDefinitions="Auto, *">
                <Grid Grid.Row="0" ColumnDefinitions="2.5*, 1.2*, 1.5*, 2*, 200" Padding="14,12"
                      BackgroundColor="{AppThemeBinding Light={StaticResource AdminFieldLight}, Dark={StaticResource AdminFieldDark}}">
                    <Label Grid.Column="0" Text="Titlu" Style="{StaticResource AdminMuted}" FontAttributes="Bold" />
                    <Label Grid.Column="1" Text="Severitate" Style="{StaticResource AdminMuted}" FontAttributes="Bold" />
                    <Label Grid.Column="2" Text="Locație" Style="{StaticResource AdminMuted}" FontAttributes="Bold" />
                    <Label Grid.Column="3" Text="Interval" Style="{StaticResource AdminMuted}" FontAttributes="Bold" />
                    <Label Grid.Column="4" Text="Acțiuni" Style="{StaticResource AdminMuted}" FontAttributes="Bold" HorizontalTextAlignment="End" />
                </Grid>

                <CollectionView Grid.Row="1" x:Name="AlertsList"
                                EmptyView="Nu există alerte care să corespundă filtrelor.">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="m:WeatherAlert">
                            <Grid ColumnDefinitions="2.5*, 1.2*, 1.5*, 2*, 200" Padding="14,10">
                                <Label Grid.Column="0" Text="{Binding Title}" Style="{StaticResource AdminText}" VerticalOptions="Center" />
                                <Label Grid.Column="1" Text="{Binding SeverityDisplay}" Style="{StaticResource AdminMuted}" VerticalOptions="Center" />
                                <Label Grid.Column="2" Text="{Binding LocationName}" Style="{StaticResource AdminMuted}" VerticalOptions="Center" />
                                <Label Grid.Column="3" Text="{Binding IntervalDisplay}" Style="{StaticResource AdminMuted}" VerticalOptions="Center" />
                                <HorizontalStackLayout Grid.Column="4" Spacing="6" HorizontalOptions="End" VerticalOptions="Center">
                                    <Button Text="Editează" WidthRequest="92" HeightRequest="34" FontSize="12"
                                            Style="{StaticResource AdminGhostButton}"
                                            CommandParameter="{Binding .}"
                                            Clicked="OnEditClicked" />
                                    <Button Text="Șterge" WidthRequest="80" HeightRequest="34" FontSize="12"
                                            Style="{StaticResource AdminDangerButton}"
                                            CommandParameter="{Binding .}"
                                            Clicked="OnDeleteClicked" />
                                </HorizontalStackLayout>
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </Grid>
        </Border>
    </Grid>
</ContentPage>
